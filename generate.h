#ifndef GENERATE_H
#define GENERATE_H

#include <stdio.h>

//  prototype for the function defined in generate2.c
void generateTable(FILE* File_write, int row_count, int count, FILE* files[16], char filenames[16][64]);

#endif

